get:
  $ref: "./retrieve_periods_of_account.yaml"
  summary: Retrieve Periods of Account  [test only]
  security:
    - User-Restricted:
        - read:self-assessment

put:
  $ref: "./create_update_periods_of_account.yaml"
  summary: Create or Update Periods of Account  [test only]
  security:
    - User-Restricted:
        - write:self-assessment